@model IEnumerable<Sample_PJ_Core.Models.Editdata.Tantousha>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
            @Html.DisplayNameFor(model => model.cTANTOUSHA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sTANTOUSHA)
        </th>*@
            <th>担当者</th>
            <th>担当者名</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cTANTOUSHA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sTANTOUSHA)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
